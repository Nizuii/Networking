# What is OSI Model.

The OSI model is a conceptual framework that explains how data moves from one device to another over a network using seven logical layers, from physical transmission (cables, signals) upto user-facing application (like browsers or email clients).
